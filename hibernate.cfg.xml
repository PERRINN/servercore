<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE hibernate-configuration PUBLIC
	"-//Hibernate/Hibernate Configuration DTD 3.0//EN"
	"http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
	<session-factory>
		<!-- Database Connection Settings -->
		<property name="connection.driver_class">com.mysql.jdbc</property>
		<property name="connection.url">jdbc://db02.oordas.net/perrapp</property>
		<property name="connection.username">USER_NAME</property>
		<property name="connection.password">USER_PWD</property>

		<!-- Use the built-in JDBC Connection Pool -->
		<property name="connection.pool_size">1</property>

		<!-- SQL Dialect -->
		<property name="dialect">org.hibernate.dialect.MySQLDialect</property>

		<!-- Use Hibernate's session context management -->
		<property name="current_session_context_class">thread</property>

		<!-- Disable the second-level cache -->
		<property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>

		<!-- FOR DEBUG ONLY Echo SQL to stdout -->
		<property name="show_sql">true</property>

		<!-- FOR DEBUG ONLY Drop and recreate the schema on startup -->
		<property name="hbm2ddl.auto">update</property>

		<!-- Mapping files -->
		<mapping resource="country.hbm.xml" />
		<mapping resource="region.hbm.xml" />
		<mapping resource="city.hbm.xml" />
	</session-factory>
</hibernate-configuration>

